<!DOCTYPE html>
<html lang="en">
  
<head>
  <title><%= "Greater Poweshiek Community Foundation: Grant Management"%></title>
  <!-- <title><%= content_for?(:title) ? "Greater Poweshiek C-omunity Foundation: Grant Management" : yield(:title) %></title> -->
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= render 'layouts/shim' %>
</head>

<body>
    <%= render 'layouts/navbar' %>
    <div class ="container">
      <div class="row">
          <!--<div class="col-lg-9" style = "margin-top: 40px;"> -->
          <div class="col-lg-12" style = "margin-top: 10px;">
             <% flash.each do |name, msg| %>
             <% if msg.is_a?(String) %>
             <div class="alert alert-dismissable alert-<%= name.to_s == 'notice' ? 'success' : 'danger' %>">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <%= content_tag :div, msg, :id => "flash_#{name}" %>
            </div>
          <% end %>
          <% end %>
 </div>
          <div class="jumbotron">
          <%= yield %>
      </div><!--jumbotron-->
          </div><!--/row-->
    </div> <!-- /container -->
 <div class="jumbotron-second">
    <div class="container">
      <div class="jumbotron-second">
      <%= render 'layouts/footer' %>
      </div>
    </div> <!-- /container -->
    </div>
</body>

</html>
